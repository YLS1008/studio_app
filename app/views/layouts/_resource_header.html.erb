<div class="navbar-nav mr-auto nav-right dropdown dropleft">
    <% if anyone_signed_in? %>
    <a class="nav-link dropdown-toggle btn btn-primary sqs-block-button-element" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
        <%= resource.full_name %>
    </a>
    <div class="dropdown-menu dropdown-menu-left" aria-labelledby="navbarDropdown">
        <%= link_to "עריכת פרופיל", edit_resource_path(resource), class: "dropdown-item btn btn-primary btn-m sqs-block-button-element" %>
        <%= link_to placeholder_path do %>
        <span class="dropdown-item btn btn-primary btn-m sqs-block-button-element"><i class="fa fa-bell">
                <span class="badge badge-info"><%= resource.notifications.count %></span>
            </i>הודעות</span>
        <% end %>
        <%= link_to "התנתקות", destroy_resource_session_path(resource),method: :delete, class: "dropdown-item btn btn-primary btn-m sqs-block-button-element" %>
    </div>
    <% else %>
    <a class="nav-link dropdown-toggle btn btn-primary" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
        התחברות
    </a>
    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
        <%= link_to "כניסת מדריכים", new_instructor_session_path, class: "dropdown-item btn btn-primary btn-m sqs-block-button-element" %>
        <%= link_to "כניסת מתאמנים", coming_soon_path, class: "dropdown-item btn btn-primary btn-m sqs-block-button-element" %>
    </div>
    <% end %>
</div>